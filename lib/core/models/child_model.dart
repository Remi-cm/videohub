import 'package:flutter/material.dart';

class Child {
  final String? id, orphanageId, name, grade, sex, contact;
  final DateTime? birthDate;

  Child({this.id, this.orphanageId, this.name, this.grade, this.sex, this.contact, this.birthDate});
}